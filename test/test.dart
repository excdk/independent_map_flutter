import 'package:independent_map/independent_map.dart';
import 'package:independent_map/src/defaults/default_map_controller.dart';

main() {
  // MapController mapController =
  //     DefaultMapController(const GeoPoint(latitude: 2, longitude: 2));
}
